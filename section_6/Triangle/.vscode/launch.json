{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Triangle",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "build triangle"
        }
    ]
}
