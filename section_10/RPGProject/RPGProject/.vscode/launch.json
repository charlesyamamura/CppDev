{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch RPG",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/rpg",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build RPG"
        }
    ]
}
